- alias: 'Sunrise'
  id: sunrise
  initial_state: true
  triggers:
    - trigger: sun
      event: sunrise
      #offset: "-00:45:00"
  #condition:
    #condition: state
    #entity_id: group.all_devices
    #state: home
  actions:
    - action: telegram_bot.send_message
      data:
        message: "Sunrise"