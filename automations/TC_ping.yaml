#
##########################################
## KEYBOARD - ping
##
- alias: 'TC ping'
  id: tc_ping
  initial_state: true
  triggers:
    - trigger: event
      event_type: telegram_command
      event_data:
        command: '/ping'
  actions:
    - action: telegram_bot.send_message
      data:
        message: "pong"
